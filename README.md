# El recorrido de los héroes

Este proyecto se generó automáticamente con la [Angular CLI](https://github.com/angular/angular-cli) versión 9.1.9.

Luego de clonar, ejecutar por única vez:
```
npm install
```